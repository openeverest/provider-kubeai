apiVersion: v1
kind: Service
metadata:
  name: {{ include "provider-kubeai.fullname" . }}
  labels:
    {{- include "provider-kubeai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "provider-kubeai.selectorLabels" . | nindent 4 }}
